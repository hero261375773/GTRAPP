package com.a8_hk.gtrapp;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.widget.ArrayAdapter;
import android.widget.Spinner;

public class OrderActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_order);

//        Spinner spinner =(Spinner)findViewById(R.id.include_exe_orders).findViewById(R.id.spinner_order_plan_date);

//        Spinner spinner = (Spinner) findViewById(R.id.spinner_order_plan_date);
//        String[] mItems = getResources().getStringArray(R.array.order_type);
//        ArrayAdapter<String> adapter=new ArrayAdapter<String>(MainActivity.activity,android.R.layout.simple_spinner_item, mItems);
//        adapter.setDropDownViewResource(android.R.layout.simple_spinner_dropdown_item);
//        spinner .setAdapter(adapter);
    }
}
